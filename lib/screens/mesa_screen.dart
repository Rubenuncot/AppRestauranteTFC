import 'package:flutter/material.dart';

class MesaScreen extends StatelessWidget {
   
  const MesaScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('MesaScreen'),
      ),
    );
  }
}